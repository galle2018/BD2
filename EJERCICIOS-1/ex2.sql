# 2. Escreva uma consulta SQL que mostre apenas os campos número (emp_no) e nome (first_name) 
# de todos os empregados (employees).

USE psdb; # la selecciono
SELECT emp_no, first_name FROM employees;